<odoo>

    <record id="auto_update_aal_from_sp" model="base.automation">
        <field name="name">=> Auto AAL from SP on stock.valuation.layer </field>
        <field name="model_id" ref="model_stock_valuation_layer"/>
        <field name="state">code</field>
        <field name="code">
#record.analytic_picking_auto()
#move = record.stock_move_id
#picking = move.picking_id
#plan = move.analytic_distribution
#status = picking.state
#code = picking.picking_type_id.code

#exist = record.analytic_line_ids
#name = record.description
#quantity = record.quantity
#uom = record.uom_id
#unitprice = record.unit_cost
#value = record.value

#if (move.id and picking.id and plan and not exist.ids and status == 'done') and (code in ['incoming','outgoing']):
#  for aa,percent in plan.items():
#    analytic = env['account.analytic.account'].search([('id','=',int(aa))])
#    new = env['account.analytic.line'].create({'name':name, 'account_id':analytic.id, 'product_id':record.product_id.id,
#      'product_uom_id':uom.id, 'unit_amount':quantity, 'amount':value * percent / 100, 'svl_id': record.id
#    })

        </field>
        <field name="trigger">on_create_or_write</field>
        <field name="filter_pre_domain"></field>
        <field name="filter_domain"></field>
        <field name="active" eval="True"/>
    </record>



</odoo>